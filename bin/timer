#!/bin/bash

minutes=30

while true; do
    echo ${minutes}m;
    sleep 60
    if [ $minutes == 1 ]; then
        notify-send -u critical Workout "now";
        echo "end";
        sleep infinity;
    else
        ((minutes--));
    fi
done
