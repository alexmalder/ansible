{% for key in inventory.all.children.keys() %}
{% for host_name in inventory.all.children[key]["hosts"] %}
{{inventory.all.children[key]["hosts"][host_name]["ansible_host"]}}
{% endfor %}
{% endfor %}
